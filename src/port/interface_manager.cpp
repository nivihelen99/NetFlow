#include "netflow++/interface_manager.hpp" // Corrected include path
#include <iostream> // For placeholder messages in callbacks (if any methods were here)

namespace netflow { // Corrected namespace

// All InterfaceManager methods are currently defined inline in the header file.
// This .cpp file is kept for potential future non-inline definitions or
// to ensure the library netflow_port_lib is created by CMake if it has no other sources.
// InterfaceManager::InterfaceManager() {
    // Constructor body could be here if not defaulted in header
// }

} // namespace netflow
